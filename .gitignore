node_modules/
dist/
.DS_Store
*.local
.vite/

# I brani completi pesano ~219 MB in totale: restano in locale, fuori dal repo.
# Servono solo per il "reveal" musicale; senza, l'app mostra titolo e artista.
# Per rigenerarli: npm run importa-media
public/media/musica/**/completo.mp3
